- @games.each do |game|
  %p
    = game.name
    %a{ href: game_url(game.id) } Show Game
    %a{ href: edit_admin_game_url(game.id) } Edit Game
    %a{ href: admin_game_users_url(game.id) } Edit Users

%a.btn.btn-secondary{ href: new_admin_game_url } New Game
