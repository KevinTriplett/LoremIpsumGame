.row
  .col
    %h5= game_name
    %p
      %strong Changes made since your last turn are highlighted

.row
  .col#ep.read-only= diff

.row
  .col
    = buttons
.row
  .col 
    Players
    %ul
      - players.each do |u|
        %li{ class: u.id == current_player_id ? "current-player" : nil }
          .square{ style: "background-color:#{u.author_color};"}
          = u.name
          = u.id == current_player_id ? " <== current player" : ""
